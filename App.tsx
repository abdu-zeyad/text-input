import { StyleSheet, Text, View } from "react-native";
import React from "react";

const App = () => {
  return (
    <View
      style={{
        padding: 100,
      }}
    ></View>
  );
};

export default App;

const styles = StyleSheet.create({});
